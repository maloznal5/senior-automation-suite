#!/bin/bash
# Запуск SAS Monitor в защищенном фоне

BOT_PATH="$HOME/senior-automation-suite/projects/sas_monitor_bot/src"

# 1. Установка tmux (если нет)
pkg install tmux -y -q

# 2. Очистка старых зависших сессий
tmux kill-session -t sas_bot 2>/dev/null

# 3. Запуск бота в отсоединенной (detached) сессии
tmux new-session -d -s sas_bot "cd $BOT_PATH && python main.py"

echo "===================================================="
echo "🚀 БОТ ПЕРЕВЕДЕН В ФОНОВЫЙ РЕЖИМ (ENTERPRISE GRADE)"
echo "===================================================="
echo "[+] Termux можно закрывать, бот продолжит работу."
echo "[+] Подключиться к экрану бота:  tmux attach -t sas_bot"
echo "[+] Отключиться (оставить в фоне): Нажми Ctrl+b, затем d"
